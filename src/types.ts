
export type note = {
  user: string,
  title: string,
  body: string,
  color: string,
}

export type JsonNote ={
  user: string,
  title: string,
  body: string,
  color: string,
}

export enum colors { 'Rojo', 'Verde', 'Azul', 'Amarillo'};
